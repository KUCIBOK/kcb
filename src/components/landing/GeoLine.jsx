/**
 * Decorative separator with diamond endpoints. Color adapts to --accent CSS var.
 */
export default function GeoLine() {
  return <div className="kcb-geo-line bg-[var(--accent,var(--kcb-or))]/12" />
}
